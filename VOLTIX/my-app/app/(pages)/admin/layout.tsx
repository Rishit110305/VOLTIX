export const metadata = {
    title: "Voltix Admin — Sign In",
    description: "Secure admin access to the Voltix EV station control center.",
};

export default function AdminLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
