export const metadata = {
    title: "Voltix Admin — Station Control Center",
    description: "Real-time EV swap station monitoring and management panel.",
};

export default function AdminPanelLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
